public class Main
{
	public static void main(String[] args) 
	{
	    
	    String s="java";
	    int n=s.length();
	    String temp="";
	    
	    for(int i=0;i<n;i++)
	    {
	        temp=s.charAt(i)+temp;
	    }
		System.out.println(temp);
	}
}
